# Add project specific ProGuard rules here.
# By default, the flags in this file are applied to the module itself during
# a release build. For rules to apply to consuming apps, put them in consumer-rules.pro.
