# Tasks

- [x] Create `settings.gradle` in `XIME.UI` directory
- [x] Create `gradle.properties` in `XIME.UI` directory
- [x] Update `build.gradle` in `XIME.UI` directory with plugin versions and hardcoded dependencies
- [/] Sync Gradle and verify the fix
